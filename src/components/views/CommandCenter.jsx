import FeatureCarousel from '../FeatureCarousel';

// The Command Center is a 3D feature carousel — each card is a platform
// pillar feature; click a card to open its working view.
export default function CommandCenter() {
  return <FeatureCarousel />;
}
